#include "main.h"

int add(int c, int n)
{
	return c + n;
}

int sub(int c, int n)
{
	return c - n;
}

int mul(int c, int n)
{
	return c * n;
}

int div(int c, int n)
{
	return c / n;
}

int mod(int c, int n)
{
	return c % n;
}
